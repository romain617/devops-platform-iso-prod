apiVersion: v1
kind: Service

metadata:
  name: {{ .Values.service.name }}

spec:
  type: {{ .Values.service.type }}

  selector:
    app: {{ .Values.name }}

  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 8081
