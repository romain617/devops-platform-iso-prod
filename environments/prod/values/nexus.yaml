name: nexus-prod

replicaCount: 1

image:
  repository: sonatype/nexus3
  tag: latest
  pullPolicy: IfNotPresent

service:
  name: nexus-prod
  type: ClusterIP
  port: 8081

ingress:
  enabled: true
  className: nginx
  host: nexus-prod.local
  path: /

persistence:
  enabled: true
  accessMode: ReadWriteOnce
  storageClass: "local-path"
  size: 10Gi

resources:
  requests:
    cpu: "500m"
    memory: "2Gi"
  limits:
    cpu: "2"
    memory: "4Gi"

securityContext:
  fsGroup: 200
