name: fastapi-prod

replicaCount: 2

image:
  repository: tiangolo/uvicorn-gunicorn-fastapi
  tag: latest
  pullPolicy: IfNotPresent

service:
  port: 80

ingress:
  enabled: true
  className: nginx
  host: fastapi-prod.local
